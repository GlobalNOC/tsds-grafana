[
{
        "name":"Main_Dashboardfor entity - Amazon",
        "unit":"bps",
        "lines_graph":True,
        "bar_graph":False,
        "points_graph":False,
	"entity":"Amazon",
	"group_by": "entity.name",
        "query": 'get intf, node, values.input, values.output, entity.name between($START, $END) by $group_by from interface where ( entity.name = "Amazon")',
	"drilldown":True,
        "drilldown_param": {
            "group_by": ["node", "intf"],
            "name": "Drilled down for Main_Dashboard entity - Amazon"
        }
}
]

